import { placePublicImage } from './placeImagePaths'

/** Last-resort fallback when primary and per-place fallback both fail to load. */
export const AP_WIDE_FALLBACK = placePublicImage('araku', 'araku-valley.jpg')
